kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: app-test-pvc
  namespace: db
  labels:
    app: app-test
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
